build:
  script: export JAVA_HOME='/opt/jdk-13.0.2/' && mvn clean initialize && mvn -Dspring.config.lcation=/opt/tomcat-dematag/conf/app_config/application.yml package && /opt/tomcat-dematag/bin/shutdown.sh && cp -rf target/esup-signature.war /opt/tomcat-dematag/webapps/ROOT.war && /opt/tomcat-dematag/bin/startup.sh
  only:
    - test
  tags:
    - test

build2:
  script: mvn clean initialize && mvn -Dspring.config.location=/opt/tomcat-esup-signature/conf/app_config/application.yml clean package && /opt/tomcat-esup-signature/bin/shutdown.sh && cp -rf target/esup-signature.war /opt/tomcat-esup-signature/webapps/ROOT.war && /opt/tomcat-esup-signature/bin/startup.sh
  only:
    - tags
  tags:
    - prod

